{
  "id": "backend/no_adhoc_logging_config",
  "surface": "backend",
  "title": "App code never configures logging globally; redaction is centralized",
  "intent": "Structured logging + PII redaction is installed once by ``configure_logging`` (called by ``create_app``). A module calling ``logging.basicConfig`` / ``dictConfig`` / ``fileConfig`` re-points logging and can silently bypass the central secret-redaction filter.",
  "layer": "static-portable",
  "enforcement": [
    {
      "kind": "build-time",
      "tool": "terp.arch",
      "ref": "check_no_adhoc_logging_config"
    }
  ],
  "opt_out": "# arch-allow-no-adhoc-logging-config: <reason>",
  "guide_topic": "capability",
  "corpus": true
}
