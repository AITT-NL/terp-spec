{
  "id": "backend/routes_declare_response_model",
  "surface": "backend",
  "title": "Every content route declares ``response_model=`` (no bare ORM/data out)",
  "intent": "Covers both decorator routes (``@router.get(...)``) and imperative registration (``router.add_api_route(...)``): a route with neither a ``response_model`` nor a no-body ``status_code`` (204/205/304) can serialize a bare ORM object out of the boundary, so both forms are checked.",
  "layer": "static-bespoke",
  "enforcement": [
    {
      "kind": "build-time",
      "tool": "terp.arch",
      "ref": "check_routes_declare_response_model"
    }
  ],
  "opt_out": "# arch-allow-routes-declare-response-model: <reason>",
  "runtime": {
    "applicability": "deferred",
    "rationale": "A boot-time route scan on the composition seam could refuse a content route without a response_model, with the same dynamic-composition fidelity the sibling _validate_router_response_models control already has (nested/imperative routers a source scan cannot follow). Today the sibling validator deliberately skips response_model=None routes, so declaring a model is held at build time only \u2014 an explicit gap, not an equivalence claim."
  },
  "guide_topic": "module",
  "corpus": true
}
