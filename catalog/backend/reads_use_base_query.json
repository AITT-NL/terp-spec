{
  "id": "backend/reads_use_base_query",
  "surface": "backend",
  "title": "A scope-trait model is read through ``base_query``, never a raw ``select()``",
  "intent": "A model that mixes :class:`~terp.core.SoftDeleteMixin` or ``TenantScopedMixin`` carries row scope (soft-delete / tenant). A bespoke read that issues ``select(<Model>)`` directly \u2014 instead of building on ``base_query()`` \u2014 drops that scope, leaking soft-deleted or cross-tenant rows (the F1 follow-up to ADR 0017: closing ``base_query`` to overrides did not close a *new* read method that never calls it). Build reads on ``base_query()`` / ``business_filters()``; the request session re-applies the scope to any single-entity ``select`` as the runtime backstop, and this rule is the build-time early warning. ``base_query`` itself \u2014 the one sanctioned ``select(model)`` \u2014 lives in the framework, not a module, so it is never scanned here.",
  "layer": "static-bespoke",
  "enforcement": [
    {
      "kind": "build-time",
      "tool": "terp.arch",
      "ref": "check_reads_use_base_query"
    }
  ],
  "opt_out": "# arch-allow-reads-use-base-query: <reason>",
  "guide_topic": "service",
  "corpus": true
}
