{
  "id": "backend/base_query_not_overridden",
  "surface": "backend",
  "title": "A service never overrides ``base_query`` \u2014 add read filters via ``business_filters``",
  "intent": "``BaseService.base_query`` composes the **non-droppable** row scope (soft-delete + every registered capability predicate, e.g. tenancy) with the service's ``business_filters``. Overriding it \u2014 the old, footgun-y seam \u2014 can silently drop soft-delete / tenant scoping the moment the override forgets ``super().base_query()``, leaking soft-deleted or cross-tenant rows. Add static conditions via ``business_filters()`` (you return conditions, not a query, so you cannot drop scope and need no ``super()``); a per-call filter belongs in a custom ``list`` that builds on ``base_query().where(...)`` (ADR 0017).",
  "layer": "static-bespoke",
  "enforcement": [
    {
      "kind": "build-time",
      "tool": "terp.arch",
      "ref": "check_base_query_not_overridden"
    }
  ],
  "opt_out": "# arch-allow-base-query-not-overridden: <reason>",
  "guide_topic": "service",
  "corpus": true
}
