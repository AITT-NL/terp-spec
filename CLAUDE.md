@AGENTS.md

> **Maintainers:** this file exists only because Claude Code reads `CLAUDE.md`,
> not `AGENTS.md`. It imports the shared agent rules above, so every agent —
> Copilot, Claude, Cursor, Codex — is held to one source of truth.
> Never add rules here: edit `AGENTS.md` instead.
